# Copy to infra/.env.production (gitignored). Replace every REPLACE_ME before starting production.
# Never commit the copied file. Production registration is closed; device auth is off.

POSTGRES_PASSWORD=REPLACE_ME
NAKAMA_CONSOLE_USERNAME=REPLACE_ME
NAKAMA_CONSOLE_PASSWORD=REPLACE_ME
